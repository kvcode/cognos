const rightPane = document.querySelector('.right-pane-cards');
console.log('Drop target:', rightPane);
console.log('pointer-events:', getComputedStyle(rightPane).pointerEvents);
console.log('position:', getComputedStyle(rightPane).position);
console.log('z-index:', getComputedStyle(rightPane).zIndex);

// Check all parents
let parent = rightPane.parentElement;
while (parent) {
  console.log('Parent:', parent.className, 'pointer-events:', getComputedStyle(parent).pointerEvents);
  parent = parent.parentElement;
}


// Check if RightPane exists at all
console.log('RightPane main:', document.querySelector('.right-pane'));

// Check all divs to see what's there
console.log('All panes:', document.querySelectorAll('[class*="pane"]'));

// Check the full container
console.log('Container:', document.querySelector('.custom-prompt-page-container'));
```

console.log('LeftPane buttons:', document.querySelectorAll('.left-pane-button'));
console.log('LeftPane container:', document.querySelector('.left-pane-container'));
```
