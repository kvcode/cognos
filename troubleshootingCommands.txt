// Check if getParameters exists on PreLoad prototype
// This will tell us if the updated file is loaded
window.testPreLoadMethod = function() {
  console.log("Testing if PreLoad.getParameters exists...");
  
  // Try to find ANY custom control instance
  var containers = document.querySelectorAll('[data-custom-control], [class*="custom"]');
  console.log("Found containers:", containers.length);
  
  return "Check console for results";
};

window.testPreLoadMethod();


// View the actual PreLoad source code in memory
console.log("Checking PreLoad source...");

// This will show us if getParameters is defined
try {
  // Look for RequireJS modules
  if (window.require && window.require.defined) {
    console.log("RequireJS modules loaded:", Object.keys(window.require.s.contexts._.defined));
  }
} catch(e) {
  console.log("Could not access RequireJS:", e);
}
